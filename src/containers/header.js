import React from 'react';

const Header = () => {
    return(
        <header className="App-header" style={{minHeight:'350px'}}>
            <span className="HeaderName">"Krupali's Blog"</span>
            {  /*  <h1 className="App-title">Welcome to React</h1> */}
        </header>
    )
}

export default Header;
